---
# Generated by Jekyll on every build. Lists the homepage plus any other
# published HTML pages (those without `sitemap: false`), then the LLM text
# endpoints. No manual URL list to maintain - new pages are picked up
# automatically.
permalink: /sitemap.xml
sitemap: false
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>{{ '/' | absolute_url }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
{% for page in site.pages %}{% assign ext = page.url | split: '.' | last %}{% unless page.sitemap == false or page.url == '/' or ext != 'html' %}  <url>
    <loc>{{ page.url | absolute_url }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
  </url>
{% endunless %}{% endfor %}  <url>
    <loc>{{ '/llms.txt' | absolute_url }}</loc>
    <lastmod>{{ site.time | date_to_xmlschema }}</lastmod>
  </url>
</urlset>
